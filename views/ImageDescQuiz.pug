extends layout

block content
    .container
        form.quiz(action="")
            h1 Ask the Question
            h2 What you can see in the image?
            img(src="/images/logo.png")
            input(type="text")
            input(type="text")
            input.btn(type="button")